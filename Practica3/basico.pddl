(define (domain basico)
	(:requirements :adl :typying)
	(:types Base Asentamiento Almacen Rover)


(:types Base - object
		Asentamiento - Base
		Almacen - Base
		Rover)

(:predicates
	(conectado ?b - Base)
)

(:action mover ?r - Base ?b - Rover

)

(:action recoger

)

(:action entregar

)

)
